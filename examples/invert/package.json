{
  "name": "@azphalt/example-invert",
  "version": "0.1.0",
  "private": true,
  "description": "Sample azphalt extension: an invert filter. Doubles as a host-contract fixture.",
  "license": "MIT",
  "author": "Az (https://hereliesaz.com)",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": { "@azphalt/sdk": "workspace:*" },
  "devDependencies": {
    "@azphalt/runtime-reference": "workspace:*",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.0.0"
  }
}
