#!/usr/bin/env bash
# Replace this with your script. `dependencies` in manifest.json lists the system packages
# (namespaced by package manager, e.g. "apt") a host should install before running it.
set -euo pipefail

echo "Hello from my-script!"
