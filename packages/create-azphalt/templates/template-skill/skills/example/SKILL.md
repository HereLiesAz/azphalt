---
name: example
description: Describe what this skill teaches an agent to do, and when to use it.
---

# Example Skill

Write the instructions an agent should follow here — the steps, the constraints, the things to check
before acting. This file is read as text by the host's agent, not executed.

See `references/notes.md` for background material and `scripts/helper.sh` for a script the agent may
invoke, if your host's skill loader supports that.
