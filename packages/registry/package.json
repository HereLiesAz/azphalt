{
  "name": "@azphalt/registry",
  "version": "0.3.2",
  "publishConfig": {
    "access": "public"
  },
  "description": "The azphalt registry: publish, index, search, and serve .azp packages — plus the consignment marketplace overlay.",
  "license": "MIT",
  "author": "Az (https://hereliesaz.com)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@azphalt/azdk": "workspace:*",
    "@azphalt/azp": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "fflate": "^0.8.3",
    "typescript": "^7.0.2",
    "vitest": "^5.0.2"
  },
  "sideEffects": false
}
